# Config

## Principio

La configuracion real de compatibilidad vive fuera del producto o en archivos heredados como `.mcp.json` y `.claude/settings.json`.

## Regla

No tocar `.mcp.json` sin verificar funcionamiento.
